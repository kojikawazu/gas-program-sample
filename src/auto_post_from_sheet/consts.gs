/**
 * スプレットシートの「シート名」
 * 
 * 使うデータのシート名を変更する場合はここを変更する。
 */
const SPREAD_SHEET_NAME = "sample";

/** 
 * X投稿時間
 * 
 * この時間にXへ投稿する。
 * 上限は10個とする。
 * 
 * 例) 6時、8時、10時、、、etc
 */
const X_POST_HOURS = [6, 8, 10, 12, 14, 16, 18, 20, 22, 0];
